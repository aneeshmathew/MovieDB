{
  "name": "moviedb",
  "private": true,
  "version": "1.0.0",
  "description": "MovieDB monorepo root — orchestrates server/ and client/. No install needed to run these scripts; they only use Node built-ins.",
  "scripts": {
    "setup": "node scripts/setup.js",
    "dev": "node scripts/dev.js",
    "build": "npm run build --prefix client",
    "test": "npm test --prefix server && npm test --prefix client"
  }
}
