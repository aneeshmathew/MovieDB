{
  "name": "moviedb",
  "private": true,
  "version": "1.0.0",
  "description": "MovieDB monorepo root — orchestrates server/ and client/. No install needed to run these scripts; they only use Node built-ins. Playwright E2E tests are the one exception: they need `npm install` at the root (see e2e/README.md).",
  "scripts": {
    "setup": "node scripts/setup.js",
    "dev": "node scripts/dev.js",
    "build": "npm run build --prefix client",
    "test": "npm test --prefix server && npm test --prefix client",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^26.2.0"
  }
}
